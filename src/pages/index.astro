---
// HOME画面
---
<!DOCTYPE html>
<html lang="ja">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>nikki-1K</title>
    <style>
        :root { --bg-color: #0d0d0d; --text-color: #00ff41; --border-color: #333; --font-main: 'Courier New', Courier, monospace; }
        body { background-color: var(--bg-color); color: var(--text-color); font-family: var(--font-main); margin: 0; line-height: 1.6; display: flex; flex-direction: column; align-items: center; min-height: 100vh; }
        header { width: 100%; max-width: 600px; padding: 2rem 1rem; border-bottom: 1px solid var(--border-color); }
        nav { margin-top: 1rem; }
        nav a { color: var(--text-color); text-decoration: none; margin-right: 1.5rem; border: 1px solid var(--text-color); padding: 2px 8px; font-size: 0.9rem; }
        nav a:hover { background: var(--text-color); color: var(--bg-color); }
        main { width: 100%; max-width: 600px; padding: 2rem 1rem; }
        .post { margin-bottom: 4rem; padding-bottom: 2rem; border-bottom: 1px dotted var(--border-color); }
        .post-date { font-size: 0.8rem; opacity: 0.7; margin-bottom: 0.5rem; }
        .post-content { white-space: pre-wrap; word-wrap: break-word; }
        h1 { font-size: 1.5rem; margin: 0; }
    </style>
</head>
<body>
<header>
    <h1>nikki-1K</h1>
    <nav>
        <a href="./">HOME</a>
        <a href="./mypage">MY PAGE</a>
    </nav>
</header>
<main>
    <div class="post">
        <div class="post-date">2026-01-13 22:15</div>
        <div class="post-content">
nikki-1K へようこそ。
ここは、1000文字以内の「語り」だけが流れる場所。
画像も、いいねも、フォローもありません。
ただ、静かに誰かの言葉と出会ってください。
        </div>
    </div>
</main>
</body>
</html>
